# ESTune
ESTune aims to early stop the execution of underperforming configurations and replace their actual performance with estimated values, thereby enhancing the tuning efficiency of existing iterative methods. This strategy is implemented through an early stopping component, which primarily consists of three parts: MAML, HBNN, and an evaluator. 
